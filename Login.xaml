<Window x:Class="Страховая.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Страховая"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="301.264">
    <Grid>
        <Grid>
            <!--фон-->
            <Grid.Background>
                <ImageBrush ImageSource="Resources/bg.png"></ImageBrush>
            </Grid.Background>
            <Grid.Effect>
                <BlurEffect Radius="25"/>
            </Grid.Effect>
        </Grid>
        <StackPanel>
            <TextBlock Text="Аскор" FontSize="50" TextAlignment="Center" Foreground="White" >
            <TextBlock.Effect>
                <DropShadowEffect BlurRadius="25"></DropShadowEffect>
            </TextBlock.Effect>
                
        </TextBlock>
            <StackPanel VerticalAlignment="Center" Height="324" Margin="20">
                <TextBlock Text="Логин" HorizontalAlignment="Center" FontSize="25" Foreground="White" />
                <TextBox Height="30" Width="250" Foreground="White" Background="#55000000" BorderBrush="#88ffffff"/>
                <TextBlock Text="Пароль" HorizontalAlignment="Center" FontSize="25" Foreground="White" />
                <PasswordBox Width="250" Height="30" Foreground="White" Background="#55000000" BorderBrush="#88ffffff" />
                <Button x:Name="login" Click="Login_Click" Content="Войти" FontSize="18" Width="90" Height="50" Margin="10" Foreground="White" Background="#55000000" BorderBrush="#33ffffff" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
